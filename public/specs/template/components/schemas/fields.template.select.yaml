allOf:
  - $ref: .\fields.template.basic.yaml
  - properties:
      widget:
        enum: 
          - select
        
      enum:
        type: array
        items:
          type: "#/properties/type" 

x-tags:
  - field-template
 