type: object
title: email identity
properties:
  properties:
    $ref: .\fields.email.yaml
  identifier-type:
    type: string
    default: email
    readOnly: true
  provider:
    type: string
    enum:
      - otp
      - lite
      - link
