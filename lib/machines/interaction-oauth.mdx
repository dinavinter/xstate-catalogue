# Interaction Form Using Hypermedia OAuth 



## Lookup
After user filled the form, before posting the interaction to user, we need to know who is the user, for that we will use <Service>lookup</Service> API to fetch the user first

<CurrentState />
 <WholeContext />
 <Spec>interaction</Spec>
 
